# 佐々木歯科医院ブログ自動投稿システム - Claude Code 実行指示書

## プロジェクト概要

佐々木歯科医院（京都市西京区）の公式ブログ記事を、医療広告ガイドラインに厳格に準拠した形で執筆・投稿するためのシステムです。Claude Code がブリーフ（記事企画書）に基づいて記事を執筆し、品質チェックを経て WordPress に下書き投稿します。

- 対象サイト: https://www.dentalclinic-sasaki.com
- 記事監修者: 佐々木 継泰 副院長（国際顎頭蓋機能学会 認定医・マスター）
- 投稿先カテゴリ: clinicblog
- 最低文字数: **8,000字以上**

---

## SEO記事執筆エージェントチーム

記事を書く際は、以下の7つの専門エージェントを起動して分業する。
ユーザーが「記事を書いて」「ブログを書いて」等と指示した場合、このワークフローを自動的に実行する。

### 🔍 Agent A: SEO Keyword Analyst（SEOキーワード分析官）

**起動タイミング**: Phase 1（Agent G と**並列**実行）
**ツール**: WebSearch, WebFetch

**タスク**:
1. ブリーフの `keyword` を受け取り、Google検索結果の上位傾向を分析する
2. 以下を調査・報告する:
   - **検索意図の分類**（情報収集型 / 比較検討型 / 行動型）
   - **上位記事の見出し構成の傾向**（H2を5〜7個が標準か、FAQ重視かなど）
   - **関連キーワード・共起語**（記事に自然に含めるべき語句リスト）
   - **京都市西京区に特化したローカルSEO要素**（地名、駅名、エリア名の使い方）
   - **差別化の余地**（上位記事に不足している情報は何か）
3. 調査結果を構造化してメインスレッドに返す
4. **取得できない情報は推測で埋めず「要確認」と明記する**

**成果物**: SEO分析レポート（メインスレッドのコンテキストに保持）

---

### 🏥 Agent G: Clinic HP Researcher（医院HP調査官）

**起動タイミング**: Phase 1（Agent A と**並列**実行）
**ツール**: WebFetch

**役割**: 公式サイトから記事テーマに関連する診療情報を取得し、記事の正確性と内部リンク計画の根拠を作る。

**タスク**:
1. 記事テーマに関連する公式サイトページを `WebFetch` で取得・解析する:
   - **テーマ直結ページ**（例: 矯正記事なら `ortho.html`、`alignment.html`）
   - **関連ページ**（例: 矯正記事なら `child.html`、`profile.html` も参照）
   - **トップページ**（`https://www.dentalclinic-sasaki.com/`）から医院の最新の方針を確認
   - **既存ブログ記事**（`/category/clinicblog/`）でテーマ重複がないか確認
2. 以下の情報を抽出・構造化して報告する:

   **A. 診療内容の具体的情報（記事に反映する素材）**:
   - 当院が該当治療で実際に行っている診療内容・手順
   - 使用している機器・技術の名称と特徴（K7マシン、トロフィーシステム等）
   - 当院独自のアプローチ（顎位矯正、ホリスティック治療、科学的検査ベース等）
   - 診療方針・理念で記事に引用できる具体的な文言
   - カウンセリングの流れや治療ステップの説明

   **B. 内部リンク計画**:
   - `clinic.json` の `servicePages` から記事テーマに関連するページを選定
   - 各ページのリンクを本文中のどの文脈で挿入すべきかの提案
   - リンクアンカーテキストの候補（自然な日本語になるように）
   - **最低3本の内部リンク**を目標とする
   - リンク先ページの内容要約（リンクの文脈が不自然にならないために）

   **C. 外部リンク計画**:
   - 記事テーマに関連する出典を `config/eeat-config.json` の `allowedSources` から選定
   - 各出典の**具体的なページURL**を調査（トップページではなく、該当する情報のページ）
   - 以下の外部リンクを計画:
     - **出典引用リンク** × 2〜3本: 厚生労働省の統計、学会のガイドライン等
     - **E-E-A-T強化リンク**: 監修者の所属学会ページ等
   - **リンク先が実在するかWebFetchで確認**（404のURLは使用しない）

   **D. 既存ブログとの差別化**:
   - 既存記事で既に書かれているテーマ・切り口を報告
   - 新記事で差別化すべきポイントを提案

3. **取得できなかった情報は「取得不可」と明記し、絶対に推測で埋めない**
4. **「MEAW」がサイト内にあっても、記事では使用禁止であることを注記する**

**成果物**: 医院HP調査レポート（診療情報 + 内部リンク計画 + 外部リンク計画 + 差別化分析）

---

### 📐 Agent B: Article Architect（記事設計官）

**起動タイミング**: Phase 2（Agent A + Agent G 両方の完了後）
**入力**: Agent A のSEO分析レポート + Agent G のHP調査レポート + ブリーフ

**タスク**:
1. SEO分析結果・HP調査結果・ブリーフの3つを統合し、**記事の詳細アウトライン**を作成する
2. アウトラインの要件:
   - **H2見出し × 6〜7個**（ブリーフの5個をベースに、SEO分析から必要に応じて追加）
   - 各H2に **H3小見出し × 2〜3個**
   - 各セクションの**目標文字数**（合計8,000字以上になるよう配分）
   - 各セクションに含めるべき**共起語・関連キーワード**
   - **内部リンク配置計画**（Agent G の提案を反映、最低3本）
   - **外部リンク配置計画**（Agent G の出典調査を反映、最低2本）
   - **FAQ構成**（5問、Schema.org FAQPage対応）
   - **当院独自情報の配置**（Agent G が取得した診療情報をどのセクションで使うか）
3. `config/shiji.md` を確認し、NG表現に抵触しない見出し表現を使う
4. 以下の SEO 構造を設計に含める:
   - タイトルタグ（32文字以内、キーワード含む）
   - メタディスクリプション（120文字以内）
   - **キーワードの自然な配置計画**（タイトル・H2・冒頭100字・まとめに含める）

**成果物**: 記事詳細アウトライン（内部リンク・外部リンク配置計画付き）

---

### ✍️ Agent C: Medical Content Writer（医療コンテンツライター）

**起動タイミング**: Phase 3（Agent B 完了後）
**入力**: Agent B のアウトライン + Agent G のHP調査レポート + ブリーフ + `config/shiji.md`

**タスク**:
1. アウトラインに沿って **8,000字以上の記事本文**を Markdown で執筆する
2. 以下のフォーマットを厳守する:

```markdown
---
title: "記事タイトル"
slug: "english-slug"
metaDescription: "メタディスクリプション（120文字以内）"
categories:
  - clinicblog
tags:
  - タグ1
  - タグ2
  - タグ3
---

## H2見出し1

本文...

### H3小見出し1

本文...

（以下、アウトラインに沿って記述）

## よくある質問

### Q. 質問文

A. 回答文...

（FAQ 5問）

## まとめ

まとめ文...

---

**治療についての重要事項**

- **治療内容**: 具体的な治療名と概要
- **費用**: ○○円〜（税込）※自由診療のため保険適用外
- **治療期間・回数の目安**: 目安を記載
- **リスク・副作用**: 具体的なリスクを列記
- ※自由診療は公的医療保険が適用されない診療です。
- ※治療効果には個人差があります。

**佐々木歯科医院**
副院長 佐々木 継泰

- 国際顎頭蓋機能学会 日本部会 認定医
- 国際顎頭蓋機能学会 マスター
- 京都歯科医療技術専門学校 運営委員
- 京都市立大枝小学校 学校医
```

3. 執筆ルール:
   - 文体は「です・ます調」で統一
   - 専門用語には平易な説明を併記（例: 「顎位矯正（あごの位置を正しい位置に戻す矯正治療）」）
   - 1文は80文字以内を目安
   - 引用は `config/eeat-config.json` の `allowedSources` のみ使用
   - 内部リンクは `clinic.json` の `servicePages` に存在するURLのみ使用
   - 「京都市西京区」「桂駅」「佐々木歯科医院」をローカルSEO要素として自然に含める
   - 冒頭100字以内にメインキーワードを含める
   - キーワードの過剰な詰め込み（keyword stuffing）は行わない
   - **内部リンク**: Agent G の計画に基づき最低3本挿入。アンカーテキストは自然な文脈で（「[当院の矯正治療について詳しくはこちら](URL)」ではなく、文中に溶け込ませる）
   - **外部リンク（出典）**: Agent G が調査した具体的ページURLを使い最低2本挿入。「〜によると」「〜では」の形式で本文に組み込む
   - **当院独自情報の活用**: Agent G が取得した診療内容・機器名・方針を正確に記事に反映する
4. 記事を `articles/` ディレクトリに保存する

**成果物**: `articles/[slug].md`

---

### 🔎 Agent D: Compliance Auditor（コンプライアンス監査官）

**起動タイミング**: Phase 4（Agent C 完了後）
**入力**: Agent C が執筆した記事ファイル + `clinic.json` + `config/shiji.md`

**タスク**:
1. `lib/quality-check.js` の全10項目を手動で確認する:
   - [ ] **1. NG語チェック**: `clinic.json` の `ngWords` に含まれる語が記事内に存在しないか
   - [ ] **2. プレースホルダー残留チェック**: `[[...]]` 形式が残っていないか
   - [ ] **3. ダミーURL チェック**: `example.com`、`dummy`、`placeholder` を含むURLがないか
   - [ ] **4. 文字化けチェック**: 文字化け（U+FFFD）が含まれていないか
   - [ ] **5. figcaption タグチェック**: `<figcaption>` タグが使用されていないか
   - [ ] **6. AI型CTAチェック**: `>>...はこちら` パターンが含まれていないか
   - [ ] **7. 文字数チェック**: 本文が**8,000字以上**あるか
   - [ ] **8. 架空内部リンクチェック**: 院内リンクが `servicePages` に存在するURLか
   - [ ] **9. 禁止固有名詞チェック**: 禁止されている固有技術名が含まれていないか
   - [ ] **10. 限定解除要件チェック**: 「リスク」「副作用」「自由診療」の3語がすべて存在するか
2. 医療広告ガイドライン追加チェック:
   - [ ] 治療効果を保証・断定する表現がないか
   - [ ] Before/After写真への言及がないか
   - [ ] 他院との比較表現がないか
   - [ ] 体験談・口コミの引用がないか
   - [ ] 引用出典が許可リストに含まれるか
3. 違反箇所があれば**具体的な修正案とともに報告**し、Agent C の記事を修正する
4. 全項目パスするまでループする

**成果物**: 修正済み記事 + コンプライアンスレポート

---

### 📊 Agent E: SEO Final Optimizer（SEO最終最適化官）

**起動タイミング**: Phase 5（Agent D 完了後）
**入力**: Agent D の修正済み記事 + Agent A のSEO分析レポート

**タスク**:
1. SEO最終チェック:
   - [ ] タイトルにメインキーワードが含まれているか
   - [ ] メタディスクリプションにキーワードが含まれ、120文字以内か
   - [ ] 冒頭100字以内にキーワードが含まれているか
   - [ ] H2見出しの少なくとも2つにキーワードまたは関連語が含まれているか
   - [ ] まとめセクションにキーワードが含まれているか
   - [ ] 共起語・関連キーワードが自然に分散配置されているか
   - [ ] 内部リンクが2本以上含まれているか
   - [ ] 京都市西京区のローカルSEO要素（地名・駅名）が含まれているか
   - [ ] **内部リンクが3本以上**含まれ、全て `servicePages` に存在するURLか
   - [ ] **外部リンク（出典）が2本以上**含まれ、全て `allowedSources` の具体ページURLか
   - [ ] 外部リンク先が実在するか（404でないか）
   - [ ] リンクのアンカーテキストが自然な日本語か（「こちら」だけのリンクは不可）
2. 読みやすさの最終チェック:
   - [ ] 導入文が患者の悩みに共感する内容か
   - [ ] 各セクションの冒頭に結論があるか（PREP法）
   - [ ] 箇条書き・表が適度に使われているか
   - [ ] 文章が長すぎるセクションがないか
3. 必要に応じて微調整（文字数を減らさない範囲で）
4. 最終版を `articles/` に保存

**成果物**: SEO最適化済み記事

---

### 🎭 Agent F: Human Voice Rewriter（人間味リライター）

**起動タイミング**: Phase 6（Agent E 完了後）
**入力**: Agent E のSEO最適化済み記事

**役割**: AIが書いた痕跡を完全に消し、歯科医院の副院長が自ら書いたとしか思えない文章に仕上げる。

**タスク**:

1. **AI臭い文章パターンの検出と除去**:
   - 「〜について解説します」「〜を見ていきましょう」「それでは〜」のような定型導入を除去
   - 「いかがでしたか？」「ぜひ参考にしてください」のような定型締めを除去
   - 「〜することが重要です」「〜と言えるでしょう」の連続使用を崩す
   - 「まず」「次に」「さらに」「また」が等間隔で並ぶ機械的な接続を崩す
   - 「〜の一つです」「〜の一環として」のような曖昧な表現を具体化する
   - 箇条書きが過剰に整い過ぎている場合、一部を地の文に変換する

2. **文体の不均一化（人間らしいリズム）**:
   - 全文が同じリズムで流れないように、短文と長文を意図的に混ぜる
   - 一部のセクションでは体言止めや倒置法を使う
   - 「〜ですね。」「〜なんです。」のような口語寄りの表現を少量（全体の5%程度）混ぜる
   - 段落の長さを不均一にする（3行の段落の後に6行の段落、その後1行など）

3. **歯科医師としての一人称視点の挿入**:
   - 「当院では〜」「私たちは〜」「診療の中で感じるのは〜」のような一人称表現を自然に散りばめる
   - 「患者さまからよく聞かれるのが〜」のような臨床経験に基づく語り口を入れる
   - 抽象論ではなく、日常の診療で起こる具体的な場面描写を加える
     例: 「初めての矯正相談でいらっしゃる方の多くが、まず治療期間を気にされます」
   - 京都の地域性に触れる表現を自然に挿入する
     例: 「桂駅周辺にお住まいの方」「西京区は住宅地が多く、お子さんの矯正相談も増えています」

4. **過剰な構造化の崩し**:
   - H3見出しの文言を「問いかけ型」「断言型」「具体型」などバリエーションをつける（全部が同じ構造にしない）
   - 箇条書きの項目数を3,4,5とセクションごとにばらけさせる
   - 全セクションが「説明→メリット→注意点」の同じパターンにならないよう、順序を変える

5. **AI検出チェックリスト**（最終確認）:
   - [ ] 同じ文末表現（「〜です。」「〜ます。」）が3回以上連続していないか
   - [ ] 全てのH2/H3見出しが同じ構文パターン（「〜とは」「〜のメリット」等）になっていないか
   - [ ] 「重要」「ポイント」「メリット」「デメリット」が過剰に繰り返されていないか
   - [ ] 導入文が「〜について解説します」パターンになっていないか
   - [ ] 各段落の冒頭が全て同じパターン（「〜は、」で始まるなど）になっていないか
   - [ ] 文章全体に歯科医師個人の視点・経験が反映されているか
   - [ ] 読んだ人が「歯医者さんが書いたブログだな」と感じるか

**成果物**: 人間味のある最終記事

---

## エージェントチーム実行フロー

```
ユーザー: 「○○の記事を書いて」

Phase 1（並列実行）:
  ├─ Agent A (SEO Keyword Analyst) → SEO分析レポート
  └─ Agent G (Clinic HP Researcher) → 医院HP調査レポート + リンク計画

Phase 2: Agent B (Article Architect)
  └→ 詳細アウトライン設計（内部リンク3本+ / 外部リンク2本+ の配置計画込み）

Phase 3: Agent C (Medical Content Writer)
  └→ 8,000字以上の記事執筆（HP情報・リンク反映済み）

Phase 4: Agent D (Compliance Auditor)
  └→ 品質チェック・リンク検証・修正

Phase 5: Agent E (SEO Final Optimizer)
  └→ SEO最終最適化・リンク最終チェック

Phase 6: Agent F (Human Voice Rewriter)
  └→ AI臭の除去・歯科医師の肉声化

Phase 7: メインスレッド
  └→ ユーザーに記事全文を提示してレビュー依頼
  └→ ユーザー承認後、publish.js で下書き投稿
```

---

## 絶対に守るべきルール

1. **publish.js をユーザーの明示的な許可なく実行してはならない**
2. `--status publish` を使用する場合は必ずユーザーに確認する
3. `clinic.json` の `ngWords` 先頭2件に該当する他院固有の矯正技術名を記事内に使用しない（大文字・小文字を問わず禁止）
4. 医療広告ガイドライン違反表現を使用しない（NG表現一覧は `config/shiji.md` 参照）
5. 架空の情報・URL を記載しない
6. 限定解除要件ブロックを記事末尾に必ず含める（自費診療に言及する場合）
7. 監修者情報を記事末尾に必ず含める
8. 引用は `config/eeat-config.json` の `allowedSources` に含まれる出典のみ使用する
9. 内部リンクは `clinic.json` の `servicePages` に存在するURLのみ使用する

---

## やってはいけないこと一覧

- 外部AI APIを呼び出すコードの追加
- `publish.js` の無断実行（ユーザーの明示的な承認が必須）
- 未確認の医院情報の捏造
- Before/After写真の掲載や言及
- 他院との比較（名指し・示唆を問わず）
- 患者の体験談・口コミの引用
- 芸能人・有名人の治療歴の示唆
- 禁止された固有技術名の使用
- 許可リスト外の出典の引用（個人ブログ、企業サイト、Wikipedia、SNS等）
- `clinic.json` の `servicePages` に存在しない架空の内部リンクの作成
- 治療効果を保証・断定する表現（「絶対治る」「必ず成功」等）
- 最上級・排他的表現（「日本一」「最高」「唯一」「京都で一番」等）

---

## ファイル構成

```
dentalBlog/
├── CLAUDE.md                  ← 本ファイル（Claude Code 実行指示書）
├── README.md                  ← セットアップ手順・利用ガイド
├── clinic.json                ← 医院設定（基本情報、NG語、サービスページ一覧）
├── publish.js                 ← WordPress投稿スクリプト（メイン実行ファイル）
├── test-wp-auth.js            ← WordPress認証テストスクリプト
├── verify-images.js           ← 画像検証スクリプト
├── package.json               ← Node.js パッケージ定義
├── .env                       ← 環境変数（WP_SITE_URL, WP_USERNAME, WP_APP_PASSWORD）
├── config/
│   ├── shiji.md               ← 医療広告ガイドライン執筆指示書
│   ├── eeat-config.json       ← E-E-A-T設定（監修者情報、引用可能出典）
│   └── tracked-keywords.json  ← SEOキーワード20件
├── briefs/
│   ├── _template.json         ← ブリーフテンプレート
│   └── *.json                 ← 各記事のブリーフファイル
├── lib/
│   ├── wordpress-api.js       ← WordPress REST API クライアント
│   ├── markdown-html.js       ← Markdown→HTML変換・frontmatterパーサー
│   ├── quality-check.js       ← 記事品質チェック（10項目の自動検証）
│   └── image-overlay.js       ← OGP画像生成（1200x630、タイトルオーバーレイ）
├── articles/                  ← 執筆した記事Markdownファイル置き場
├── output/                    ← 生成されたOGP画像の出力先
└── wp-plugin/                 ← WordPress認証修正プラグイン
    └── rest-api-auth-fix/
```

---

## トラブルシューティング

### publish.js 実行時に「Quality check FAILED」が出る

`lib/quality-check.js` が検出したエラーを確認し、該当箇所を修正してください。よくある原因:

- NG語（`clinic.json` の `ngWords`）が記事内に含まれている
- 文字数が8,000字未満
- 限定解除要件の3語（「リスク」「副作用」「自由診療」）のいずれかが欠けている
- プレースホルダー `[[...]]` が残っている

### publish.js 実行時に「Authentication failed」が出る

1. `WP_SITE_URL` が正しいか（末尾スラッシュなし）
2. `WP_USERNAME` が正しいか
3. `WP_APP_PASSWORD` が有効な Application Password か
4. WordPress に REST API Auth Fix プラグインがインストール・有効化されているか
5. `node test-wp-auth.js` で認証テストを実行して詳細を確認

### 文字数が8,000字に満たない

- 各H2セクションに1,000〜1,500字を確保する
- FAQセクションの回答を200〜300字ずつ充実させる
- 具体例・ケーススタディ・比較表を追加する
- ただし、水増しではなく患者にとって有用な情報を追加すること
